#include<iostream>
using namespace std;
#include<bits/stdc++.h>

int main() {
    // Let me understant pointer
    // suppose i have intgers i=3;
    // and if if create a pointer it will point to the address of i where 3 is stored 
    int arr[10]={1,99,3,4};
    cout<<*(arr+1)<<endl;
    return 0;
}